# AGENTS.md

本倉庫為香港樓宇撰寫傳記式時間線。內容為繁體中文；檔案/欄位名稱保留原文。

## 工作流程

對兩份 XML 樓宇清單逐一處理，每筆 `Record` 一輪。**由 `新界/bnb-nt.xml` 開始**（先於 `港島及九龍/bnb-u.xml`），並優先處理 `浪翠園` 與 `葵俊苑`。

1. 讀取樓宇名稱與地址（見下方「資料來源」）。
2. 依 `準則.md` 撰寫故事，存入 `{新界,港島及九龍}/樓宇#{N}：{地址}.md`（見「檔案命名」）。
3. 更新 `香港樓宇故事.md` 索引（見「索引格式」）。
4. `git add` → `commit` → `push` 到 `dev-001`（見「Git 與管線」）。

## 資料來源

- 樓宇清單取自 DATA.GOV.HK，已存於倉庫：
  - `新界/bnb-nt.xml`（新界，約 137k 行）
  - `港島及九龍/bnb-u.xml`（港島及九龍，約 223k 行）
- 兩檔皆為 `<data-set>` 包含多個 `<Record>`。每筆欄位：
  - `ChineseBuildingName1`／`EnglishBuildingName1`（另有 `2`／`3`，通常空）→ 取第一個非空者；標題優先用中文名（準則 §4）。
  - `ChineseAddress1`／`EnglishAddress1`（另有 `2`／`3`）→ 取第一個非空者作 `{地址}`。
  - `YearBuild`：`-` 表示不詳 → 索引中寫 `（不詳）`；否則為西元年。
- XML 極大，勿整檔讀入；用串流/解析逐筆處理。

## 檔案命名

- 路徑：`{新界|港島及九龍}/樓宇#{N}：{地址}.md`
- `#` 後無空格；`：` 為全形冒號；`{N}` 為自增計數，`{地址}` 取自該筆 Record 的地址欄位。
- 計數 `{N}` 採**全倉庫單一遞增**（跨兩個地區資料夾共用），自 `1` 起，避免索引碰撞。新條目續接現有最大編號 +1。
- 地區歸屬由來源 XML 決定：`bnb-nt.xml` → `新界/`；`bnb-u.xml` → `港島及九龍/`。
- **地址淨化**：原始地址含檔案系統不安全字元（空格、`︰`、`：`、`，`、`,`、`、` 等），須換成 `_` 後方可用於檔名。
  - 例：`地下︰ 蒲崗村道135-143號， 蒲田里2號及 毓華里16號 樓上各層︰ 蒲田里2號` → `地下_蒲崗村道135-143號_蒲田里2號及_毓華里16號_樓上各層`。
  - 連續不安全字元合併為單一 `_`；首尾不留 `_`。
  - 索引連結與 front matter `address` 仍用原始未淨化地址；淨化僅施於檔名。

## 索引格式（`香港樓宇故事.md`）

- 含兩張表，**皆按 `落成年份` 倒序**。
- **格式（一）**：摘要計數表，欄位 `地區｜落成年份｜樓量`。空年份與 `（不詳）` 各自獨立一列。
- **格式（二）**：明細表，欄位 `地區｜落成年份｜地址｜故事`。`故事` 欄為連結 `[樓宇#{N}：{地址}](<地區>/樓宇#{N}：{地址}.md)`。
- 連結路徑須對應所屬地區資料夾（`新界/` 或 `港島及九龍/`）。注意：現存模板中「港島及九龍」列誤連到 `新界/`，撰寫時應修正為正確地區路徑。

## 寫作規範（`準則.md`，權威來源）

撰寫前必讀 `準則.md`。關鍵且易漏：

- **可信度等級 CL0–CL5**：每項關鍵陳述須標 CL。CL5 可直接陳述；CL4 用「據」；CL3 用「待考」；CL2 用「傳說」；CL1 用「推測」；**CL0 不得寫入正文**。
- **不得虛構**：無來源即標「待考」或留空，不可編造對白、場景、心理。 寧缺勿造假。
- **參考資料排序**：`CL1 → CL5` 升序；同 CL 內按字母（中文按漢語拼音）升序；每項須標 `[CLx]` 並含年份，無年份寫「不詳」；CL0 不得列入公開參考資料。
- **條目結構**：依準則 §5 / §13 模板，含 front matter（YAML）與各標準小節（摘要、基本資料、誕生、建築描述、生命歷程、大事年表、人物、城市脈絡、爭議、現況、文化再現、考證註釋、圖集、相關條目、參考資料）。資料不足可略，但保留標題。
- **時間**：西曆；不確定用 `c.`、`約`、`？`；同年份差異並列。
- **語氣**：繁體中文，中立克制；禁廣告語、情緒化、未證實因果。
- **私隱**：在世非公眾人物住址、身份、家庭、健康資訊須匿名或省略。
- 每次更新須改 front matter 之 `last_updated`。

## Git 與管線

- 工作分支：**`dev-001`**（目前所在）。勿直接推 `dev` 或 `main`。
- 自動合併管線 `.github/workflows/auto-merge.yml`：推 `dev-001` → 自動合併到 `dev` → 再自動合併到 `main`。串行化（`concurrency` 不取消進行中作業）。
- 管線依賴 repo secret `GIT_PUSH_TOKEN`（Fine-grained PAT，Contents 讀寫）。若未設定，合併會失敗——此為環境前提，非本倉庫可修。
- 每完成一筆樓宇即提交（add 該條目檔 + 更新後的索引），commit message 沿用既有風格。

## 環境備註

- 本倉庫於容器內可能觸發 git「dubious ownership」；首次執行 git 前先：
  `git config --global --add safe.directory /workplace/projects/testcase-hk-building-timeline`
- 無 build/test/lint 指令——純文件倉庫。驗證方式：檢查檔案命名、索引排序、CL 標註是否符合準則。
